<% if card.image.attached? %>
  <%= button_to card_image_path(card), method: :delete, class: "btn",
        data: { controller: "tooltip", bridge__overflow_menu_target: "item", bridge_title: t("cards.image.remove_background") } do %>
    <%= icon_tag "picture-remove" %>
    <span class="for-screen-reader"><%= t("cards.image.remove_background") %></span>
  <% end %>
<% elsif !card.closed? %>
  <%= form_with model: card, data: { controller: "form" } do |form| %>
    <label class="card-perma__image-btn btn input--file" data-controller="tooltip" data-bridge--overflow-menu-target="item" data-bridge-title="<%= t('cards.image.add_background') %>">
      <%= icon_tag "picture-add" %>
      <span class="for-screen-reader"><%= t("cards.image.add_background") %></span>

      <%= form.file_field :image, class: "input",
            accept: "image/png, image/jpeg, image/jpg, image/webp",
            aria: { label: t("cards.image.add_background") },
            data: { action: "upload-preview#previewImage form#submit", upload_preview_target: "input" } %>
    </label>
  <% end %>
<% end %>
